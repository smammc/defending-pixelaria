class PlayerCard extends Card {
  constructor(name, attack, defense, gameScreen, width, height, imgSrc) {
    super(name, attack, defense, gameScreen, width, height, imgSrc);
  }

  // Characteristic that belong only to the player card
}
